<!-- AUTO-GENERATED by benchmark_vss_analyze.py — DO NOT EDIT MANUALLY -->
<!-- Regenerate: make -C benchmarks vss-analyze -->

# Vector Search Benchmarks

## Engines

| Library | Engine | Method | Strategy |
|---------|--------|--------|----------|
| [muninn](https://github.com/neozenith/sqlite-muninn) | **muninn-hnsw** | HNSW graph index | Approximate, O(log N) search |
| [sqlite-vector](https://github.com/sqliteai/sqlite-vector) | **sqlite-vector-quantize** | Product Quantization | Approximate, O(N) scan |
| [sqlite-vector](https://github.com/sqliteai/sqlite-vector) | **sqlite-vector-fullscan** | Brute-force | Exact, O(N) scan |
| [vectorlite](https://github.com/1yefuwang1/vectorlite) | **vectorlite-hnsw** | HNSW via hnswlib | Approximate, O(log N) search |
| [sqlite-vec](https://github.com/asg017/sqlite-vec) | **sqlite-vec-brute** | Brute-force KNN | Exact, O(N) scan |

## Datasets

| Dataset | Source | Passages | Topology |
|---------|--------|----------|----------|
{% for ds in datasets -%}
| [{{ ds.label }}]({{ ds.url }}) | {{ ds.source }} | {{ ds.passages }} | {{ ds.topology }} |
{% endfor %}

## Models

| Model | Dimension | Use Case |
|-------|-----------|----------|
{% for model in models -%}
| [{{ model.model_id }}]({{ model.url }}) | {{ model.dim }} | {{ model.use_case }} |
{% endfor %}

{% for dataset in datasets %}
## Search Latency — {{ dataset.label }}

{% for model in models %}
### {{ model.dim }}d-{{ model.label }}

```plotly
--8<-- "benchmarks/charts/tipping_point_{{ model.label }}_{{ dataset.key }}.json"
```

{% endfor %}
### Cross-Model Comparison

```plotly
--8<-- "benchmarks/charts/model_comparison_{{ dataset.key }}.json"
```

{% endfor %}
## Dataset Comparison

{% for model in models %}
### {{ model.label }}

```plotly
--8<-- "benchmarks/charts/dataset_comparison_{{ model.label }}.json"
```

{% endfor %}
{% for dataset in datasets %}
## Recall — {{ dataset.label }}

```plotly
--8<-- "benchmarks/charts/recall_models_{{ dataset.key }}.json"
```

{% endfor %}
{% for dataset in datasets %}
## Insert Throughput — {{ dataset.label }}

```plotly
--8<-- "benchmarks/charts/insert_throughput_models_{{ dataset.key }}.json"
```

{% endfor %}
{% for dataset in datasets %}
## Storage — {{ dataset.label }}

```plotly
--8<-- "benchmarks/charts/db_size_models_{{ dataset.key }}.json"
```

{% endfor %}
