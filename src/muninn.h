/*
 * muninn.h — sqlite-muninn extension public API
 *
 * A single-dependency SQLite extension (powered by llama.cpp) combining:
 * - HNSW vector similarity search (CREATE VIRTUAL TABLE ... USING hnsw_index)
 * - Graph traversal table-valued functions (graph_bfs, graph_shortest_path, etc.)
 * - GGUF text embeddings via llama.cpp (muninn_embed, muninn_models VT)
 * - Node2Vec graph embeddings (node2vec_train)
 */
#ifndef MUNINN_H
#define MUNINN_H

#define MUNINN_VERSION "0.1.0"

#endif /* MUNINN_H */
