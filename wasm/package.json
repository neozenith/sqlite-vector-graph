{
  "name": "muninn-wasm-demo",
  "private": true,
  "scripts": {
    "test:e2e": "npx playwright test",
    "format": "npx prettier --write '*.{html,js,css}'",
    "format:check": "npx prettier --check '*.{html,js,css}'"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "prettier": "^3.5.0"
  }
}
