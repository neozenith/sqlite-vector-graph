# {{ title }}

{{ description }}

> Both embedding functions use **llama.cpp** under the hood to run **GGUF**-format
> embedding models. This benchmark compares their end-to-end performance.
{%- for table in tables %}

## {{ table.title }}

| {% for col in table.columns %}{{ col }} | {% endfor %}
|{% for col in table.columns %}---------|{% endfor %}
{% for row in table.rows -%}
| {% for col in table.columns %}{{ row[col] }} | {% endfor %}
{% endfor %}
{%- endfor %}
{% if chart_groups %}
{% for group in chart_groups %}

## {{ group.title }}
{% for chart in group.charts %}

### {{ chart.title }}

```plotly
--8<-- "{{ charts_dir }}/{{ chart.name }}.json"
```
{% endfor %}
{% endfor %}
{% else %}

## Charts

Charts are generated by `python -m benchmarks.harness.cli analyse --category embed`.
{% for chart in charts %}

### {{ chart.title }}

```plotly
--8<-- "{{ charts_dir }}/{{ chart.name }}.json"
```
{% endfor %}
{% endif %}
